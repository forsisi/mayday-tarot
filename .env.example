# Optional: enables AI tarot/music reading through GLM-4-Flash.
# The app still runs without this key and falls back to local mock answers.
GLM_API_KEY="your_glm_api_key_here"

# Optional: Express server port. Defaults to 3000.
PORT=3000
